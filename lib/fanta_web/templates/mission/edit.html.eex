<h2>Edit mission</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_mission_path(@conn, :update, @user_id, @mission) %>

<span><%= link "Back", to: user_mission_path(@conn, :index, @user_id) %></span>
